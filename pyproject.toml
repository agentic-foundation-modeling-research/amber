[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "amber"
version = "0.1.0"
description = "RL training infrastructure for web agents"
authors = [{ name = "Amber contributors" }]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.12"
dependencies = [
    "amber-core==0.1.0",
    "amber-env-server==0.1.0",
]

[project.urls]
Homepage = "https://github.com/agentic-foundation-modeling-research/amber"
Documentation = "https://github.com/agentic-foundation-modeling-research/amber/tree/main/docs"
Repository = "https://github.com/agentic-foundation-modeling-research/amber"
Issues = "https://github.com/agentic-foundation-modeling-research/amber/issues"

[project.optional-dependencies]
dev = [
    "amber-core[dev]==0.1.0",
    "amber-env-server[dev]==0.1.0",
]

[tool.setuptools]
packages = []

[tool.uv]
default-groups = []

[tool.uv.workspace]
members = [
    "packages/core",
    "packages/env_server",
]

[tool.uv.sources]
amber-core = { workspace = true }
amber-env-server = { workspace = true }
